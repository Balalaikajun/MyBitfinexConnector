<UserControl x:Class="TestHQ.UI.Views.BalanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TestHQ.UI.ViewModel;assembly=TestHQ.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:BalanceViewModel}"
             d:DesignWidth="800" Height="25">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal">
                    <Label Content="BTC:" />
                    <TextBox Width="80" Text="{Binding BTCBalance, StringFormat=G, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="XRP:" />
                    <TextBox Width="80" Text="{Binding XRPBalance, StringFormat=G, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="XMR:" />
                    <TextBox Width="80" Text="{Binding XMRBalance, StringFormat=G, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="DASH:" />
                    <TextBox Width="80" Text="{Binding DASHBalance, StringFormat=G, Mode=TwoWay}" />
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Валюта:" VerticalAlignment="Center" />
                <ComboBox Width="100" Margin="5,0"
                          ItemsSource="{Binding Currencies, Mode=OneTime}"
                          SelectedItem="{Binding SelectedCurrency, Mode=TwoWay}" />
                <Label Content="Итого:" VerticalAlignment="Center" />
                <TextBox Width="100" Margin="5,0"
                         Text="{Binding TotalBalance, StringFormat=G, Mode=OneWay}" IsReadOnly="True" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>